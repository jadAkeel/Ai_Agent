# Codex Rules

Machine-local Codex rule files are intentionally not copied into this repo.

`~/.codex/rules/default.rules` can contain broad shell approvals, absolute paths,
old project names, or commands that are safe only on one machine. Keep that file
local unless you manually review and sanitize every rule.

Use this directory for sanitized examples only.
